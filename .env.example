# ── Server ──────────────────────────────────────────────────────────────
PORT=3000

# ── Admin Auth ──────────────────────────────────────────────────────────
# Must match the secret stored on the Cloudflare Worker (not used by Worker anymore)
ADMIN_KEY=change-me-before-deploy

# ── Cloudflare Account ──────────────────────────────────────────────────
CF_ACCOUNT_ID=your-account-id
CF_API_TOKEN=your-api-token

# ── Cloudflare KV ───────────────────────────────────────────────────────
CF_KV_NAMESPACE_ID=your-kv-namespace-id

# ── Cloudflare R2 (S3-compatible) ───────────────────────────────────────
CF_R2_BUCKET=moodspace-templates
CF_R2_ACCESS_KEY_ID=your-r2-access-key-id
CF_R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
# Endpoint format: https://<ACCOUNT_ID>.r2.cloudflarestorage.com
CF_R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com

# ── Cloudflare Cache Purge ──────────────────────────────────────────────
CF_ZONE_ID=your-zone-id

# ── Supabase (not yet active — for future user auth & data storage) ─────
SUPABASE_URL=https://djcfqtrbfjaykdyperpf.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# ── Template Synchronization (Option 2 - Dynamic Sync) ──────────────────
# 后端通过 GitHub API 远程抓取模板时需要的配置

# 1. 你的 GitHub 用户名 (必须)
TEMPLATES_REPO_OWNER=XShen-Jason

# 2. 你的模板仓库名称 (必须)
TEMPLATES_REPO_NAME=MoodSpace-Templates

# 3. GitHub 个人访问令牌 (可选)
# 如果你发现同步报错 "API rate limit exceeded"，请填入此项
# GITHUB_TOKEN=你的github_token_在这里

# 4. 本地文件路径备份 (可选)
# 只有当你决定把模板代码也克隆一份放在 VPS 磁盘上时才需要设置
# TEMPLATES_LOCAL_PATH=/opt/MoodSpace-Templates/src